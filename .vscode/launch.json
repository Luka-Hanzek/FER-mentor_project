{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug create_geometries.py",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/scripts/python/create_geometries.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "args": [
                "--src-file", "${input:srcFile}",
                "--out-geom-dir", "${input:outGeomDir}"
            ]
        },
        {
            "name": "Debug run_benchmark.py",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/scripts/python/run_benchmark.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "args": [
                "--config", "${input:configFile}"
            ]
        }
    ],
    "inputs": [
        {
            "id": "srcFile",
            "type": "promptString",
            "description": "Path to input OSM / PBF file"
        },
        {
            "id": "outGeomDir",
            "type": "promptString",
            "description": "Output directory for GeoParquet files",
            "default": "/path/to/output"
        },
        {
            "id": "configFile",
            "type": "promptString",
            "description": "Path to yaml configuration file",
            "default": "${workspaceFolder}/bench_config.yaml"
        }
    ]
}
